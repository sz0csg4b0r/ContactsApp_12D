<Window x:Class="ContactsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsApp"
        mc:Ignorable="d"
        Title="Kapcsolatok" 
        FontSize="22"
        WindowStartupLocation="CenterScreen"
        Background="Lavender"
        Height="700" Width="800">
    <StackPanel>
        <Button x:Name="newContactBtn"
                Content="Új kapcsolat"
                Margin="10,30,10,10"
                Click="newContactBtn_Click"
                ></Button>
        <TextBlock Text="Szűrés" Margin="10,0,10,0"></TextBlock>
        <TextBox x:Name="filterBox" Margin="10"
                 TextChanged="filterBox_TextChanged"
                 ></TextBox>
        <ListView x:Name="contactsList" Height="400"
                  Margin="10"
                  SelectionChanged="contactsList_SelectionChanged"
                  >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock 
                            FontSize="20"
                            FontWeight="Bold"
                            Text="{Binding Path=Name}"
                                   >
                        </TextBlock>
                        <TextBlock
                            FontSize="15"
                            Foreground="DodgerBlue"
                            Text="{Binding Path=Email}"
                            ></TextBlock>
                        <TextBlock 
                            FontSize="15"
                            FontStyle="Italic" 
                            Text="{Binding Path=Phone}">
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
            
            
        </ListView>
    </StackPanel>
</Window>
